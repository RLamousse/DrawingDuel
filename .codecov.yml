codecov:
  bot: coverage-bot
  branch: dev

comment:
  behavior: new
  require_changes: true

coverage:
  status:
    project:
      client:
        threshold: null
        paths:
          - client/src/**/*
      server:
        threshold: null
        paths:
          - server/app/**/*
    patch: no
  range: 70..100
  round: down
  precision: 2

parsers:
  javascript:
    enable_partials: yes
