import {expect} from "chai";

describe("A service validating if there is a difference at a coord for a game", () => {
    it("should throw if the point is out of bounds", () => {
        return expect(false).to.be.false;
    });
});
